version: '4'

services:
  traefik:
    image: traefik:v2.10
    command:
      - "--api.insecure=true"
      - "--providers.docker=true"
      - "--providers.docker.exposedbydefault=false"
      - "--entrypoints.api.address=:5050"
      - "--entrypoints.web.address=:80"
    ports:
      - "80:80"
      - "5050:5050"
      - "8080:8080" # For the Traefik dashboard (optional)
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock

  frontend:
    env_file:
      - ./frontend/.env
    build:
      context: frontend
      dockerfile: Dockerfile
    restart: always
    ports:
      - 3000:3000
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.frontend.rule=PathPrefix(`/`)"
      - "traefik.http.routers.frontend.entrypoints=web"
      - "traefik.http.services.frontend.loadbalancer.server.port=3000"

  backend-core:
    env_file:
      - ./backend/.env
    build:
      context: backend
      dockerfile: Dockerfile
    command: uvicorn main:app --host 0.0.0.0 --port 5050
    restart: always
    volumes:
      - ./backend/:/code/
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.backend-core.rule=PathPrefix(`/`)"
      - "traefik.http.routers.backend-core.entrypoints=api"
      - "traefik.http.services.backend-core.loadbalancer.server.port=5050"

  backend-chat:
    env_file:
      - ./backend/.env
    build:
      context: backend
      dockerfile: Dockerfile
    command: uvicorn chat_service:app --host 0.0.0.0 --port 5050
    restart: always
    volumes:
      - ./backend/:/code/
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.backend-chat.rule=PathPrefix(`/chat`)"
      - "traefik.http.routers.backend-chat.entrypoints=api"
      - "traefik.http.services.backend-chat.loadbalancer.server.port=5050"

  backend-crawl:
    env_file:
      - ./backend/.env
    build:
      context: backend
      dockerfile: Dockerfile
    command: uvicorn crawl_service:app --host 0.0.0.0 --port 5050
    restart: always
    volumes:
      - ./backend/:/code/
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.backend-crawl.rule=PathPrefix(`/crawl`)"
      - "traefik.http.routers.backend-crawl.entrypoints=api"
      - "traefik.http.services.backend-crawl.loadbalancer.server.port=5050"

  backend-upload:
    env_file:
      - ./backend/.env
    build:
      context: backend
      dockerfile: Dockerfile
    command: uvicorn upload_service:app --host 0.0.0.0 --port 5050
    restart: always
    volumes:
      - ./backend/:/code/
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.backend-upload.rule=PathPrefix(`/upload`)"
      - "traefik.http.routers.backend-upload.entrypoints=api"
      - "traefik.http.services.backend-upload.loadbalancer.server.port=5050"
